node_modules
.next
out
*.tsbuildinfo
next-env.d.ts

# Generated by `next dev`; this repo documents its conventions in README.md and
# the root CLAUDE.md, so the auto-written ones are noise.
AGENTS.md
CLAUDE.md

# Blender preview renders judge form during authoring; the shipped assets are the .glb files.
public/scene/*.preview.png

# Blender runs the scene scripts with its own bundled Python.
__pycache__/
*.pyc
